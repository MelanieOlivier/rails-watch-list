<p id="notice"><%= notice %></p>

<div class="container">
  <h1>Movie Lists</h1>
  <table class="table">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @lists.each do |list| %>
        <tr>
          <td><%= list.name %></td>
          <td><%= link_to 'Show', list, class:"btn btn-success" %></td>

        </tr>
      <% end %>
      <%= cl_image_tag("eus2lwrrpftevqn6xdwb", width: 400, height: 300, crop: :fill) %>
    </tbody>
  </table>

  <br>

  <%= link_to 'Add a list', new_list_path, class:"btn btn-dark" %>

</div>
